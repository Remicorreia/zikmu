<form method="post" action="{{ path('app_sous_statistique_delete', {'id': sous_statistique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sous_statistique.id) }}">
    <button class="btn">Delete</button>
</form>
