{% extends 'base.html.twig' %}

{% block title %}SousStatistique{% endblock %}

{% block body %}
    <h1>SousStatistique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sous_statistique.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ sous_statistique.designation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sous_statistique_index') }}">back to list</a>

    <a href="{{ path('app_sous_statistique_edit', {'id': sous_statistique.id}) }}">edit</a>

    {{ include('sous_statistique/_delete_form.html.twig') }}
{% endblock %}
