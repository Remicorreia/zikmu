{% extends 'base.html.twig' %}

{% block title %}Artiste{% endblock %}

{% block body %}
    <h1>Artiste</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artiste.id }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ artiste.photo }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ artiste.nom }}</td>
            </tr>
            <tr>
                <th>Style</th>
                <td>{{ artiste.style }}</td>
            </tr>
            <tr>
                <th>Gestuel</th>
                <td>{{ artiste.gestuel }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ artiste.genre }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ artiste.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_artiste_index') }}">back to list</a>

    <a href="{{ path('app_artiste_edit', {'id': artiste.id}) }}">edit</a>

    {{ include('artiste/_delete_form.html.twig') }}
{% endblock %}
